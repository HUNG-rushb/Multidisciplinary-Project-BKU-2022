import { View, Text, StyleSheet, Button } from "react-native";

const LivingroomScreen = (props) => {
  return <Button title="asdf"></Button>;
};

const styles = StyleSheet.create({
  header: {
    width: "100%",
  },
});

export default LivingroomScreen;
